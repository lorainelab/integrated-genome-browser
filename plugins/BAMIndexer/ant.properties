name=Bookmark
project=Bookmark
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${lib.dir}/swing-layout.jar:${lib.dir}/jide.jar:${genoviz}/build:${osgi.impl}
plugin-jar=true
