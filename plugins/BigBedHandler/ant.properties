name=BigBedHandler
project=BigBedHandler
classpath=${igb_service}/build:${genometry}/build:${common}/build:${lib.dir}/BigWig.jar:.${lib.dir}/tribble.jar:${jar_extensions}/tribble/build:${osgi.impl}
plugin-jar=true
