name=BigWigHandler
project=BigWigHandler
classpath=${igb_service}/build:${genometry}/build:${genoviz}/build:${igb}/build:${common}/build:${lib.dir}/colt.jar:${lib.dir}/BigWig.jar:.${lib.dir}/tribble.jar:${jar_extensions}/tribble/build:${osgi.impl}
plugin-jar=true
