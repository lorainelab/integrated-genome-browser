name=External View
project=ExternalView
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${lib.dir}/genoviz-1.0.jar:${osgi.impl}:${lib.dir}/igbSwingExt-1.0.jar
test.classpath=${classpath}:${ExternalView}/build:${ExternalView}/build-test
report.classpath=${classpath}
plugin-jar=true