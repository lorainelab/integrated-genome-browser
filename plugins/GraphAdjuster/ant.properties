name=Graph Adjuster
project=GraphAdjuster
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${genoviz}/build:${lib.dir}/felix.jar:${lib.dir}/swing-layout.jar
plugin-jar=true
