name=Graph Tab
project=GraphTab
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${genoviz}/build:${osgi.impl}:${lib.dir}/swing-layout.jar:${lib.dir}/jide.jar:${Thresholding}/build
plugin-jar=true
