name=NCBIBlast
project=NCBIBlast
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${osgi.impl}/:${lib.dir}/swing-layout.jar:${ApolloLibrary}/build
plugin-jar=true
