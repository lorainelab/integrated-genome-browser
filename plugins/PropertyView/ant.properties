name=Property View
project=PropertyView
classpath=${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${genoviz}/build:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true
