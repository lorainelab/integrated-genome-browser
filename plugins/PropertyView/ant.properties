name=Property View
project=PropertyView
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${lib.dir}/genoviz-1.0.jar:${lib.dir}/jide.jar:${osgi.impl}:${lib.dir}/igbSwingExt-1.0.jar
report.classpath=${classpath}
plugin-jar=true
