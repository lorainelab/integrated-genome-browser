name=Property View
project=PropertyView
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${genoviz}/build:${lib.dir}/jide.jar:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true
