name=Property View
project=PropertyView
classpath=${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${genoviz}/build:${lib.dir}/felix.jar
report.classpath=${classpath}
plugin-jar=true
