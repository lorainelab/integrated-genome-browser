name=Restriction Sites
project=RestrictionSites
classpath=${common}/build:${igb_service}/build:${window_service}/build:${lib.dir}/genoviz-1.0.jar:${genometry}/build:${igb}/build:${osgi.impl}/:${lib.dir}/igbSwingExt-1.0.jar
report.classpath=${classpath}
plugin-jar=true