name=Restriction Sites
project=RestrictionSites
classpath=${igb_service}/build:${window_service}/build:${genoviz}/build:${genometry}/build:${root.dir}/felix.jar
report.classpath=${classpath}
plugin-jar=true