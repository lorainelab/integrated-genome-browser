name=Restriction Sites
project=RestrictionSites
classpath=${common}/build:${igb_service}/build:${window_service}/build:${genoviz}/build:${genometry}/build:${igb}/build:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true