name=Restrictions Sites
project=RestrictionsSites
classpath=${igb_service}/build:${window_service}/build:${genoviz}/build:${genometry}/build:${lib.dir}/felix.jar
report.classpath=${classpath}
plugin-jar=true