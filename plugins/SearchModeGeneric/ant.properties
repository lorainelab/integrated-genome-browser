name=SearchModeGeneric
project=SearchModeGeneric
classpath=${igb_service}/build:${common}/build:${genoviz}/build:${genometry}/build:${igb}/build:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true