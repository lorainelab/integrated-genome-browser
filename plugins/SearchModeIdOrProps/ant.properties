name=SearchModeIdOrProps
project=SearchModeIdOrProps
classpath=${igb_service}/build:${common}/build:${genoviz}/build:${genometry}/build:${SearchModeGeneric}/build:${igb}/build:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true