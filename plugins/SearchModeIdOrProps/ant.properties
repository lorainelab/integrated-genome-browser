name=SearchModeIdOrProps
project=SearchModeIdOrProps
classpath=${igb_service}/build:${common}/${lib.dir}/genoviz-1.0.jar/build:${genometry}/build:${SearchModeGeneric}/build:${igb}/build:${osgi.impl}:${lib.dir}/igbSwingExt-1.0.jar
report.classpath=${classpath}
plugin-jar=true