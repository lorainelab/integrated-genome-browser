name=SearchModeIdOrProps
project=SearchModeIdOrProps
classpath=${igb_service}/build:${genoviz}/build:${genometry}/build:${igb}/build:${lib.dir}/felix.jar
report.classpath=${classpath}
plugin-jar=true