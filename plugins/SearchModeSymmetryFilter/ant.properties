name=SearchModeSymmetryFilter
project=SearchModeSymmetryFilter
classpath=${igb_service}/build:${lib.dir}/genoviz-1.0.jar:${genometry}/build:${igb}/build:${common}/build:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true