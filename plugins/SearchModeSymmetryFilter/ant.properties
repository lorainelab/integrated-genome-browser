name=SearchModeSymmetryFilter
project=SearchModeSymmetryFilter
classpath=${igb_service}/${lib.dir}/genoviz-1.0.jar/build:${genometry}/build:${igb}/build:${common}/build:${osgi.impl}:${lib.dir}/igbSwingExt-1.0.jar
report.classpath=${classpath}
plugin-jar=true