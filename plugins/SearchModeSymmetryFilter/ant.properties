name=SearchModeSymmetryFilter
project=SearchModeSymmetryFilter
classpath=${igb_service}/build:${genoviz}/build:${genometry}/build:${igb}/build:${common}/build:${osgi.impl}
report.classpath=${classpath}
plugin-jar=true