name=Survey
project=Survey
classpath=${igb_service}/build:${window_service}/build:${common}/build:${genometry}/build:${lib.dir}/genoviz-1.0.jar:${osgi.impl}:${lib.dir}/igbSwingExt-1.0.jar
plugin-jar=true