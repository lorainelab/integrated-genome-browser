name=Track Operations
project=TrackOperations
classpath=${common}/build:${igb_service}/build:${window_service}/build:${igb}/build:${genometry}/build:${genoviz}/build:${osgi.impl}:${lib.dir}/swing-layout.jar:${lib.dir}/jide.jar
plugin-jar=true
