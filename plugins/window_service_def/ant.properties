name=Window Service Default Implementation
project=window_service_def
classpath=${common}/build:${window_service}/build:${igb_service}/build:${igb}/build:${genometry}/build:${osgi.impl}:${lib.dir}/igbSwingExt-1.0.jar:${lib.dir}/genoviz-1.0.jar
plugin-jar=true