name=Window Service Default Implementation
project=window_service_def
classpath=${window_service}/build:${igb_service}/build:${igb}/build:${genoviz}/build:${genometry}/build:${osgi.impl}
plugin-jar=true